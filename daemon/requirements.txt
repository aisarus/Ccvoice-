# S0 (обязательное)
websockets>=13

# S0 (для реальной работы, не для тестов)
claude-agent-sdk>=0.1     ; python_version >= "3.10"   # цель code
anthropic>=0.40                                        # цель chat

# разработка
pytest>=8
jsonschema>=4
